{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "start:prod": "npm run build && lerna run start:prod",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
